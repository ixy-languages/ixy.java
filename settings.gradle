// Set the project name
rootProject.name = 'ixy.java'

// Include the rest of the projects and override their names
include 'pci', 'memory'
findProject(':pci')?.name    = 'ixy.pci'
findProject(':memory')?.name = 'ixy.memory'
